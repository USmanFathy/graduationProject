name: laravel

on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 2

    - name: FTP Deploy
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: s3.serv00.com
        username: f8664_usman
        password: UsmanAhmed12

        protocol: ftp
        server-dir:  /usr/home/usmanahmed/domains/usmanahmed.serv00.net/public_html/
        local-dir: ./
        exclude: .git*, .env, .github, storage/*.key
  
